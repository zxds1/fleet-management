// packages/mobile/index.js
// Expo entry point. Registers the root component with the Expo runtime.
import { registerRootComponent } from 'expo';
import App from './src/App';

registerRootComponent(App);
